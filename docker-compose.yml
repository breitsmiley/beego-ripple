version: '3.5'

services:
  app-ripple-go:
    build:
      context: .
      dockerfile: /docker/go/Dockerfile
    ports:
      - "80:8080"
    volumes:
      - type: bind
        source: .
        target: /go/src/beego-ripple